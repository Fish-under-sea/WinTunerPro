# 开发规范

> 本文档说明项目规则文件的分布与各自职责。项目规则统一存放于 `.cursor/rules/` 目录。

## 规则文件职责

- `project-overview.mdc`：产品定位、技术栈、分层架构与目录职责（始终生效）。
- `code-organization.mdc`：代码组织、命名约定、进程边界约束（作用于 `src`/`scripts`/`tests`）。
- `engineering-standards.mdc`：构建、测试、提交、发布工程规范。
