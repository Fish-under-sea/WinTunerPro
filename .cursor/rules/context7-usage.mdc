---
description: |context7-usage
  编码/技术类任务 → 调用 user-context7 MCP 查文档（API用法、报错处理、框架使用、配置项、版本迁移等）。
  闲聊/已有项目代码/Shell命令 → 不调用。
alwaysApply: false
---
# Context7 工具使用规范

## 核心原则

**先查文档，再答问题**。当问题涉及任何不熟悉的 API、参数、行为时，必须调用 context7 查证。

---

## 一、必须调用 context7 的场景（硬性要求）

以下场景出现时，**立即调用 context7**，不要凭记忆回答：

| 场景 | 判断依据 |
|------|----------|
| 使用某库的特定 API/方法/函数 | 不确定参数签名、返回值类型 |
| 询问某框架/库的版本特性 | 不确定该版本是否支持某功能 |
| 遇到报错信息 | 不确定错误原因或解决方案 |
| 配置项、参数选项不熟悉 | 不确定可选值或默认值 |
| 迁移/升级场景 | 不确定新旧版本差异 |
| 涉及async/await、Promise、流式等异步行为 | 不确定执行顺序或生命周期 |
| 涉及权限、作用域、上下文绑定 | 不确定 this/作用域如何工作 |

---

## 二、应主动调用 context7 的关键词

当问题中出现以下关键词时，**默认应查文档**：

- `怎么用`、`如何使用`、`用法`、`参数`、`选项`、`配置`
- `最新`、`版本`、`升级`、`迁移`、`deprecated`
- `报错`、`异常`、`失败`、`错误`
- `最佳实践`、`推荐`、`规范`
- `不支持`、`有没有`、`能不能`
- `API`、`SDK`、`库`、`框架`、`模块`

**决策口诀**：遇到不确定的用法 → **先查再答**。

---

## 三、绝对禁止调用 context7 的场景

| 场景 | 原因 |
|------|------|
| 闲聊、一般性问答 | 无需外部文档 |
| 分析已提供的文件内容 | 已有上下文 |
| 数学计算、逻辑推导 | 纯推理任务 |
| 文件操作（读取/写入/删除） | 直接用工具 |
| Git、Shell 命令执行 | 直接用工具 |
| 询问项目内部代码 | 已有上下文 |
| 用户明确要求"基于你的知识回答" | 用户选择 |

---

## 四、决策流程

```
用户提问
    │
    ▼
┌─────────────────────────┐
│ 问题涉及代码/技术内容？  │
└─────────┬───────────────┘
          │
    ┌─────┴─────┐
    │           │
   否           是
    │           │
    ▼           ▼
  不调用   ┌─────────────────────────┐
          │ 你对这部分内容有100%     │
          │ 把握，不需要查文档确认？  │
          └─────────┬───────────────┘
                    │
              ┌─────┴─────┐
              │           │
             是           否
              │           │
              ▼           ▼
          不调用      立即调用
                      context7
```

---

## 五、调用方式

调用 `user-context7` MCP 工具查询文档：

```json
{
  "server": "user-context7",
  "toolName": "查询具体技术栈的文档",
  "arguments": { "query": "具体问题描述" }
}
```

每次调用前先确认工具 schema（参数名、参数类型必填）。

---

## 六、其他说明

- **文档时效性**：context7 提供最新文档，查到后优先使用其内容
- **混合任务**：如果一个问题包含多个子问题，只要有一个子问题涉及不熟悉的 API，就整体查文档
- **规则优先级**：本规则与全局语言规范同级，冲突时以本规则为准
