---
name: Skill Dispatch
description: UI/UX 技能分流规则——判断何时使用 react-bits，何时使用 ui-ux-pro-max
---

# UI/UX 技能分流规则

## 核心原则

**两个技能职责分明，不得同时激活：**

- **react-bits**：专门用于 React 前端页面开发中的动效组件需求
- **ui-ux-pro-max**：用于所有其他 UI/UX 设计规范、设计系统、配色方案、非 React 技术栈

## 判断决策树

```
用户请求 UI/UX 相关工作
│
├── 是否是 React 前端页面的【动效/动画组件】需求？
│   ├── 是（需要 Aurora/BlurText/SpotlightCard/Dock 等组件代码）
│   │   → 使用 react-bits skill
│   │   → 不使用 ui-ux-pro-max（除非用户同时需要设计规范）
│   │
│   └── 否（属于以下任一场景）
│       ├── 需要设计系统（配色/字体/布局规范）
│       ├── 需要 UX 最佳实践/可访问性指南
│       ├── 需要数据可视化/图表推荐
│       ├── 落地页结构/转化策略咨询
│       ├── 非 React 技术栈（Vue/Svelte/Flutter/SwiftUI 等）
│       ├── 需要技术栈最佳实践（Next.js/Vue/Svelte 等）
│       └── 需要交付前检查清单验证
│           → 使用 ui-ux-pro-max skill
```

## 触发 react-bits 的关键词

> 以下任一关键词出现时，优先使用 react-bits skill

| 关键词 | 说明 |
|-------|------|
| react-bits / reactbits | 直接指定框架 |
| Aurora / BlurText / SpotlightCard 等 | 指定具体组件 |
| 极光背景 / 粒子效果 / 聚光灯效果 | 指定动效效果 |
| 文字淡入 / 解密动画 / 故障风 | 指定文字动画 |
| 3D倾斜卡片 / 玻璃效果 / 瀑布流 | 指定交互组件 |
| mac底部导航 / Dock栏 | 指定导航组件 |
| Hero首屏 / 落地页（需要代码） | 页面开发 |
| 科技感 / 赛博朋克 / 极简商务（+ 代码） | 风格 + 开发 |
| "做个页面" / "做个 landing page" | 直接要求开发 |
| animated component / 动效组件 | 指定动画需求 |

## 触发 ui-ux-pro-max 的关键词

> 以下任一关键词出现时，使用 ui-ux-pro-max skill

| 关键词 | 说明 |
|-------|------|
| 设计系统 / design system | 需要系统级规范 |
| 配色方案 / 颜色搭配 / 调色板 | 配色咨询 |
| 字体搭配 / 字体推荐 | 字体咨询 |
| UX / 用户体验 / 交互体验 | UX 规范 |
| 可访问性 / 无障碍 / a11y | 可访问性 |
| 数据可视化 / 图表类型 / dashboard | 数据展示 |
| 技术栈最佳实践（Vue/Svelte/Flutter 等） | 非 React 栈 |
| shadcn / nextjs / vue / svelte | 指定技术栈 |
| 交付前检查 / 检查清单 | 质量验证 |
| 产品设计 / App 设计（非 React） | 产品设计 |
| 组件设计规范（按钮/表单/卡片等） | 组件规范 |

## 冲突解决

### 场景1：同时包含两端关键词

例如："帮我做个 React 落地页，科技感，要 Aurora 背景，还要一套配色规范"

**解决策略**：
1. 先用 **react-bits** 生成动效组件代码
2. 再用 **ui-ux-pro-max** 补充配色方案和字体搭配
3. 两者同时激活，各司其职

### 场景2：落地页/landing page

| 需求 | 使用 |
|-----|------|
| "帮我做个落地页，要动效代码" | react-bits |
| "帮我设计一个落地页的转化策略和结构" | ui-ux-pro-max |
| "落地页，要有 Aurora 背景 + 转化优化建议" | 两者同时激活 |

### 场景3：React 组件设计

| 需求 | 使用 |
|-----|------|
| "React 卡片组件，要 3D 倾斜效果" | react-bits |
| "React 组件设计规范，按钮/表单/导航" | ui-ux-pro-max |
| "React 落地页，动效 + 规范" | 两者同时激活 |

## 简化判断

**遇到 UI/UX 问题时，先问自己：用户是要"生成动效组件代码"还是"获取设计规范/建议"？**

- 只要是要生成 `.tsx`/`.jsx` 动效组件代码 → react-bits
- 只要是要配色方案/字体/UX 规范/设计系统 → ui-ux-pro-max
- 两者都要 → 同时激活
