---
name: WinTuner Pro 工程规范
description: WinTuner Pro 的构建、运行、测试、提交与发布约定，涉及工程流程、脚本命令、打包、CI、测试或提交时参考
alwaysApply: false
---

# 工程规范（构建 / 测试 / 提交 / 发布）

> 提交信息的中文规范细则见 `chinese-commit-conventions` 技能，本文件不重复，仅约定本项目的工程流程与项目专属补充。

## 1. 运行环境

- 开发与运行平台：**Windows**（功能强依赖 Windows API、WMI、注册表、PowerShell），不追求跨平台。
- Shell：PowerShell（5.1 或 7.x）。命令中含空格的路径必须加引号。
- 需要 Node.js（LTS）+ npm；PowerShell 脚本调试需以管理员身份运行。
- 大量系统操作需管理员权限，调试涉及系统写入的功能前注意先做系统还原点 / 备份。

## 2. 构建与脚本约定

- 包管理统一用 **npm**（除非团队另行约定），lockfile（`package-lock.json`）需提交。
- `package.json` 中约定标准脚本入口（具体实现随脚手架补全）：
  - `dev`：启动开发模式（Vite + Electron 热重载）
  - `build`：编译渲染进程与主进程
  - `package`：electron-builder 产出 Windows 安装包
  - `lint` / `format`：ESLint + Prettier
  - `test`：运行测试
- 依赖版本**不要凭空臆造**；新增依赖用包管理器安装最新稳定版，再提交 lockfile。

## 3. 测试约定

- TS 侧使用 Vitest（与 Vite 生态一致）做单元测试，测试放 `tests/` 或与源码同级 `*.test.ts`。
- 纯函数（数据解析、品牌匹配、参数校验）必须有单元测试，这是回归保护重点。
- PowerShell 脚本变更需在目标机型/虚拟机实测，**禁止**仅靠静态阅读就判定系统级脚本可用；高风险脚本（重装、注册表批量写入）必须在可还原的虚拟机中验证。
- 涉及 bug 修复或异常行为时，遵循 `systematic-debugging`、`test-driven-development` 技能流程。

## 4. 提交与分支

- 提交信息遵循 Conventional Commits（`feat` / `fix` / `refactor` / `docs` / `test` / `chore` / `perf` / `style`），主题行祈使语气、单一意图，描述用简体中文。
- 建议按功能模块组织提交（如 `feat(oem): 新增联想 Legion 增强模式切换`）。
- 分支策略以团队约定为准；未约定时功能开发走特性分支，避免直接推主干。
- 一次提交只做一个逻辑主题，便于审查与回滚。

## 5. 资源与安全

- `resources/` 下的离线包（驱动、运行库、镜像、字体）通常体积大且可能有版权限制，**默认不纳入 Git**（见 `.gitignore`），改用外部分发或 LFS，仓库内仅保留目录占位与清单说明。
- 不得提交任何真实密钥、产品密钥、EV 代码签名证书、私钥；用占位符与安全的密钥管理机制。
- 用户数据与备份位于 `%AppData%\WinTunerPro`，不进仓库。

## 6. 发布

- 通过 electron-builder 产出带 manifest（`requireAdministrator`）的 Windows 安装包。
- 正式发布需 EV 代码签名以降低杀软误报（见 PRD 风险章节），并准备各安全平台白名单申请。
