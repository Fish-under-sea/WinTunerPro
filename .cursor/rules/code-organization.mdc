---
name: WinTuner Pro 代码组织与命名约定
description: WinTuner Pro 的代码放置规则、命名约定与跨进程边界约束，编写 src 或 scripts 下代码时遵循
globs: src/**,scripts/**,tests/**
alwaysApply: false
---

# 代码组织与命名约定

> 适用于 `src/`、`scripts/`、`tests/` 下的代码。目录职责见 `project-overview.mdc`。

## 1. 进程边界（最重要）

Electron 三类代码严格分离，禁止互相 import 越界：

- **renderer（渲染进程）**：纯 UI 与状态，只能通过 `window.electronAPI`（preload 暴露）与主进程通信。**禁止** `import` `electron`、`fs`、`child_process` 等 Node/系统模块。
- **preload（预加载）**：唯一桥梁，用 `contextBridge.exposeInMainWorld` 暴露**白名单 API**，不得直接把 `ipcRenderer` 整体透传。
- **main（主进程）**：承载所有系统操作与 PowerShell 调度。IPC 处理器只做参数校验 + 调用 `services`，业务逻辑放 `services`。

共享的类型、常量、IPC 通道名放在 `src/shared/`，三端均可 import。

## 2. 命名约定

### TypeScript / React

- 文件：组件用 `PascalCase.tsx`（如 `DashboardPage.tsx`）；非组件模块用 `camelCase.ts`（如 `gpuService.ts`）。
- React 组件名、类型/接口名用 `PascalCase`；变量、函数用 `camelCase`；常量用 `UPPER_SNAKE_CASE`。
- IPC 通道名集中定义在 `src/shared/constants`，用 `模块:动作` 格式（如 `gpu:detect`、`oem:apply-mode`），不在调用处写裸字符串。
- 类型定义集中在 `src/shared/types`，按模块拆文件（如 `gpu.ts`、`oem.ts`、`backup.ts`）。

### PowerShell 脚本

- 文件名用 `动词-名词.ps1` 的 PascalCase（PowerShell 习惯），如 `Get-VideoController.ps1`、`Set-NvidiaPreset.ps1`、`Switch-OemMode.ps1`。
- 函数遵循 PowerShell 标准 `Verb-Noun`（用 `Get-Verb` 认可的动词）。
- OEM 脚本按品牌拆分（如 `scripts/oem/Lenovo.ps1`、`Asus.ps1`）；公共能力（备份、日志、WMI 封装）放 `scripts/common/`。
- 所有脚本必须以结构化方式输出（推荐 `ConvertTo-Json`）供主进程解析，进度信息单独成行便于管道实时捕获。

## 3. 跨进程数据契约

- 主进程 ↔ 渲染进程之间传递的数据结构必须在 `src/shared/types` 定义并双方复用，禁止两端各写一份。
- PowerShell 返回给主进程的结构，需要在 `src/shared/types` 有对应 TS 类型，主进程负责解析与校验后再回传渲染进程。

## 4. 安全与稳健性硬性要求

- 任何写注册表 / 系统服务 / 电源 / 驱动的脚本，执行前必须先调用 `scripts/common` 的备份函数生成 `.reg` 或快照。
- 渲染进程绝不拼接命令字符串传给主进程执行；主进程对来自 IPC 的参数做白名单校验，避免命令注入。
- 脚本要可重入、可中断恢复（尤其重装/续执行链路），失败要返回明确错误码而非静默吞掉。

## 5. 注释与文档

- 注释用简体中文，只解释「为什么」与非显而易见的约束（如某注册表键的含义、品牌接口的坑），不写复述代码的废话。
- 每个模块（gpu / oem / system 等）应在 `docs/` 下有一份说明，记录涉及的注册表项、WMI 类、厂商接口及风险点。
